{
  "type": "object",
  "required": ["queries"],
  "properties": {
    "queries": {
      "type": "array",
      "title": "SQL Queries",
      "description": "List of SQL statements to execute in order.",
      "minItems": 1,
      "items": {
        "type": "string",
        "format": "editor",
        "options": {
          "editor": {
            "mode": "text/x-sql",
            "lineNumbers": true,
            "lint": true
          }
        }
      }
    }
  }
}
